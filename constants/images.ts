/**
 * Minden kép asset innen hivatkozódik — képernyőben vagy komponensben
 * közvetlen `require`/`import` nem lehet.
 *
 * A négy karakter szándékosan nem kép: `<View>` elemekből épül fel
 * (lásd `components/characters/`).
 */

export const images = {};
